load("@rules_cc//cc:defs.bzl", "cc_binary")

cc_binary(
    name = "mapeditor",
    srcs = ["src/main.c"],
    deps = [
        "//packages/common:common_headers",
    ],
    linkopts = ["-lm"],
    visibility = ["//visibility:public"],
)
